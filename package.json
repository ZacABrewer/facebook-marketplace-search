{
  "name": "facebook-marketplace-search",
  "private": true,
  "version": "0.1.0",
  "description": "Search, map, group and monitor Facebook Marketplace listings with cross-checked relevance and alerts.",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web & wait",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "typecheck": "npm run typecheck --workspace=server && npm run typecheck --workspace=web",
    "test": "npm test --workspace=server"
  },
  "engines": { "node": ">=20" }
}
