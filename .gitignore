node_modules/
dist/
data/
server/data/

# Local config. .env.example is the committed template.
.env
.env.*
!.env.example

# An exported Facebook session is account-level access — never commit one.
# The README puts cookies.json in this folder, so keep it ignored.
cookies.json
cookies.txt
cookies*.json
*.cookies

*.log
*.sqlite
*.sqlite-*
.DS_Store
